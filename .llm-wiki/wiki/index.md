# Knowledge Base Index

## concepts

Архитектурные и бизнес-концепции проекта: слои, цены, доставка, API, импорт, кеш.

| Article | Summary | Updated |
|---------|---------|---------|
| [Архитектура приложения: слои и путь запроса](concepts/architecture-layers.md) | Слои (FormRequest → Controller → Cache → Preconditions → Action → Response), принципы, домены (включая Booking), морф-мапа, аутентификация (панель для перенесённых разделов, sanctum — для остатка admin API, клиент записи телефон-first), enums, корзина, домены Storage | 2026-09-17 |
| [Админ-панель на Filament](concepts/admin-panel-filament.md) | Решение о переходе с React SPA на Filament; каркас: панель /panel, session-guard admin, FilamentUser (is_active), AdminRoleCode + policies, нативные уведомления; политика слоёв (запись через Actions); PanelAction для DomainException; волны 0–2d: справочники, товары (Tire/Wheel), изображения, импорт, остатки складов (только чтение — правка из панели убрана 2026-09-12), акции и снос их admin API, выбор города в списке шин (цена/срок/наценка, ?cityId=); навигация-кластер «Каталог» + группы поднавигации (привязка через $cluster), topNavigation, снос экранов Cities/Countries; грабля: конфигурация таблицы кешируется в фазе гидратации — состояние страницы, прочитанное в table()/configure(), отстаёт на рендер (лечится пересборкой в updatedXxx); фильтры-характеристики листинга шин — штатными SelectFilter (AboveContent), контракт HasLabel у энумов панели; кластер «Шиномонтаж» (7 ресурсов, рубли в формах, Wireable для Money); удаление шины из списка убрано (у дисков осталось), страница импорта — в группе «Управление»; листинг слотов — фильтр периода из SlotPeriod (дефолт «Сегодня» ставит страница, сброс отдаёт всю сетку хронологией, прошедшие дни срезаны), колонка «Клиенты»; карточка слота — заголовок с датой и часом, таблица записей с добавлением переходом на создание; карточка записи — состав повторителем в самой форме (relation manager не видит несохранённый радиус), цена услуг с правилами следует за прайс-кубом при смене радиуса/типа, итог в схеме и при сохранении, поле «Время» 14:00–14:59 по часу слота, три поля ФИО клиента (имя и фамилия обязательны, правка меняет карточку клиента); листинг слотов — колонка «Состояние» кругами (открыт зелёный, закрыт красный), генерация сетки — кнопкой на расписании недели; третий кластер «Настройки» (Settings и Admins); грабли: дата в whereBetween, валидирующий getState, Money-атрибут без поля в форме, regexify и UTF-8, Select валидирует значение по опциям (validation.in), замыкание whereHas получает билдер; заголовки страниц — русские ярлыки ресурсов ($modelLabel/$pluralModelLabel) и подмена строк Filament «Создание: :label» / «Изменение: :label» (падеж), флаг $hasTitleCaseModelLabel против ucwords («Наценки На Доставку»), стража UiTitlesTest; карточка услуги — прайс-правила таблицей (relation manager, форма и таблица общие с разделом, уникальность куба на поле «Радиус»), запрет удаления услуги с привязками через Precondition + PanelAction (ADR 0025), поиск по радиусу по цифрам строки («R17» и «17»); кнопка «Сохранить и закрыть» на страницах правки (трейт SavesAndCloses, возврат по previousUrl, редирект внутри save() — halt и ошибки валидации оставляют на форме; у карточки слота возврат всегда в список слотов — разрыв круга «запись → слот → запись», фильтры листинга переживают); форма записи без поля «Слот» (слот приходит адресом со страницы слота, начало часа подставляется, дата слота — в заголовке правки); фильтр периода в слотах и записях — селект пресетов и календари «С»/«По» (календаря с выделением диапазона у Filament нет; класс PeriodFilter общий на два листинга), живая фильтрация без «Применить», стартовый вид «текущая неделя», 50 строк, в записях порядок «дата слота, затем время»; группа «Хранение» в кластере «Шиномонтаж» (StorageContracts) | 2026-09-17 |
| [Бизнес-модель: цепочка движения товара](concepts/business-model.md) | Завод → склад → компания → точка выдачи/курьер; сущности и их видимость клиенту | 2026-08-19 |
| [Заказ: жизненный цикл](concepts/order-lifecycle.md) | Статусная машина заказа (pending → paid → … → delivered), отмена и возврат, кто меняет статусы | 2026-08-19 |
| [Каталог: ценообразование](concepts/catalog-pricing.md) | Полная цена города: цепочка наценок, MarkupRuleMatcher, применение акций (PromotionMatcher/PromotionDiscount, base_price), пересчёт catalog_prices от готовой stocks.price, округление; граница «снимок vs расчёт на лету» (ADR 0011) | 2026-09-11 |
| [Сроки доставки](concepts/delivery-times.md) | Графики отгрузки, delivery_min/max, кто считает срок (DeliveryInfoService на лету vs DeliveryStockSelector/DeliveryTimeCalculator для листинга), контракт enrichProduct (связь delivery может отсутствовать), фильтрация по бакетам | 2026-09-11 |
| [Публичный API каталога](concepts/public-catalog-filter-api.md) | Справочник городов (/api/reference/city) + фасеты и листинг шин (/api/reference/filter/tire, /api/catalog/tires) и дисков (/api/reference/filter/wheel, /api/catalog/wheels): контракт фильтров, учёт города, meta.seo, euro_label, origin, агрегация цены, кеш с индексом ключей и JSON-roundtrip (ADR 0004) | 2026-08-21 |
| [Импорт каталога из XLSX](concepts/xlsx-import-pipeline.md) | Пайплайн upload → ImportMasterJob → чанки → ChunkJob, маппинг XLSX → БД (description на модель, origin-колонки, евро-лейбл, SEO-формулы name/slug), инвалидация; точка входа — страница панели /panel/catalog/import | 2026-09-10 |
| [Запись на шиномонтаж: домен Booking](concepts/booking-domain.md) | Перенос из tireslot (ADR 0012): 10 таблиц booking_*, единый клиент users (телефон-first); доменный слой — PriceCalculator (точное совпадение, 422), SlotAvailabilityReader, BookingCodeService (TTL, одноразовость), ConfirmBooking (транзакция + lockForUpdate, 409), GenerateSlotGrid + slots:generate (15 мин), SMS-job; публичный API /api/booking (Scramble); админка — кластер «Шиномонтаж» (7 ресурсов, CreateAdminBooking — закрытие не барьер); Money VO (ADR 0013); ФТ-карта и список нереализованного — tz/booking-functional-requirements.md; листинг слотов в панели — фильтр периода и колонка «Клиенты», карточка слота с записями; payload Money в Livewire — массив; правка записи оператором (ФТ-19) — состав строками в форме, цена услуг с правилами следует за кубом, итог пересчитывается из строк (Action UpdateAdminBooking); время внутри часа и закрытие часа (час занимает только запись ровно на начало, две записи на одно время невозможны — EnsureSlotTimeIsFree под блокировкой), статусы без arrived; клиент — ФИО по частям (name/surname/patronymic, склейка для показа, сайт по-прежнему одним именем); создание записи — той же формой, что правка (состав с ценой и итогом, цены из формы, серверный пересчёт только у сайта); услуга — прайс-правила в её карточке, удаляется только неиспользуемая (правила/записи/комплексы держат FK-запрет, ADR 0025); листинги слотов и записей — период календарём (селект пресетов + «С»/«По»), стартовый вид «текущая неделя», порядок записей «дата слота, затем время»; соседний домен Storage — договор хранения на клиента, без связи с записью | 2026-09-17 |
| [Хранение колёс: домен Storage](concepts/storage-domain.md) | Отдельный домен Storage (ADR 0026): договор на клиента — срок от/до, стоимость снимком (копейки, MoneyCast), позиции (наименование + особенности), статус active/closed с датой выдачи; панель — группа «Хранение» в кластере «Шиномонтаж», повторитель со связью, каскад позиций | 2026-09-17 |
| [Эксплуатация](concepts/operations.md) | Команды Makefile, окружение (правка .env требует пересоздания контейнеров; локаль APP_LOCALE=ru; SMS_* для кодов записи), планировщик (promotions:sync, slots:generate), очередь и импорты (фактические Job'ы), runbooks | 2026-09-11 |
| [Кеширование](concepts/caching.md) | Cache Service до Action, Observer-инвалидация (шины и диски), TTL, индекс ключей, JSON-roundtrip сериализация (ADR 0004) | 2026-08-21 |

## entities

Сущности БД каталога и географии.

| Article | Summary | Updated |
|---------|---------|---------|
| [Шина (TireProduct)](entities/tire-product.md) | tire_products: поля (включая euro_label, origin_id), сезоны, индексы, парсинг размеров, связи, SEO-формулы name/slug (слово сезона — из метки энума), описание на модели | 2026-09-11 |
| [Диск (WheelProduct)](entities/wheel-product.md) | wheel_products: тип материала, геометрия (PCD/ET/DIA), касты decimal:1, парсинг при импорте, публичный каталог, slug без точек, origin_id | 2026-08-21 |
| [Происхождение товара (ProductOrigin)](entities/product-origin.md) | product_origins: jsonb {badge, description} ×3, UNIQUE-триплет, заполнение из origin-колонок XLSX (OriginParser/OriginResolver), origin_id товаров, вывод origin в листингах | 2026-08-21 |
| [Запись catalog_prices](entities/catalog-price.md) | Пара stock × city: полная цена и сроки, UNIQUE, пересчёт и инвалидация | 2026-08-19 |
| [Город и география](entities/geo-city.md) | Регионы, города (с slug и публичным справочником /api/reference/city: region, meta.default), точки выдачи, city_price_rules, city_delivery_times | 2026-08-20 |
